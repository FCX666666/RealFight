export class Person {
    name: string;
    id: string;
    price: number;
    constructor(name: string, id: string, price: number) {
        this.name = name
        this.id = id
        this.price = price
    }
}